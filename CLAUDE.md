# CLAUDE.md — 사주트립 (SajuTrip)

이 파일은 저장소에서 작업하는 Claude/개발자를 위한 프로젝트 가이드다.
근거 문서: [PRD] 사주트립 (Notion). 스펙 충돌 시 최신 PRD가 우선한다.

## 1. 프로젝트 개요

외국인 관광객의 **성별·생년월일** 최소 입력만으로 사주(오행) 프로필을 산출하고,
"부족한 기운을 채우는 한국 여행"이라는 프레임으로 관광지·웰니스·체험 상품을 추천하는 웹 서비스.
인기 랜드마크에 편중된 인바운드 관광객을 맥락 있게 지방으로 분산시키는 것이 목표.

- 여행 전: 오행 프로필 → 부족한 기운 안내 → 목적지·동선 설계
- 여행 중: 위치·일정에 맞춰 동선에 반복 개입 (강제성 없음)
- 여행 후: 채운 기운을 요약한 공유 콘텐츠(이미지) 생성 → SNS

### 범위 (Phase 1)
- **In scope**: Web only, 영어 1개 언어, **회원가입(P1)** — 사주 결과·일정·엘레먼트 수집을 저장하기 위해 scope-in. 유저 데이터 저장소는 **Airtable**.
- **Out of scope**: 앱 네이티브 개발, 한국관광공사 다국어 서비스 API를 통한 다국어 대응
- 비즈니스 모델: 어필리에이트(여기어때·Klook 등 딥링크 커미션) + 배너 광고(Google Ads). 자체 결제 인프라 없음.

### 공모전 제약 (중요)
- **심사 기간 동안 모든 외부 API는 실시간 호출로 동작해야 한다.** (배치 캐시만으로 응답하면 안 됨)
- 이를 위해 배치/실시간을 전환하는 env 플래그를 둔다 → 아래 6.5 참조.

## 2. 기술 스택

| 영역 | 선택 | 비고 |
| --- | --- | --- |
| 프레임워크 | **Next.js (App Router)** 풀스택 단일 앱 | 프론트 + API를 한 앱에서 |
| 언어 | **TypeScript** (프론트·백엔드 통일, strict) | `any` 금지 |
| 런타임 | Node.js | 서버 로직은 Route Handler / Server Component |
| DB | **Airtable** | 장소·사주 데이터 저장. `airtable` SDK를 서버에서만 사용 |
| 배포 | **Vercel** | zero-config. 서버리스 함수 특성 유의(6.5) |
| 배치 | Google Apps Script (백오피스) | 저장소 밖 Google 환경에서 구동. 스크립트 원본은 `scripts/apps-script/`에 버전 관리 |
| 스타일 | (미정 — 8/12 미팅에서 확정) Tailwind CSS 권장 | 확정 전까지 임의 도입 금지 |

## 3. 외부 API

키는 공공데이터포털(data.go.kr) 서비스별 활용신청. **모든 외부 호출은 서버 사이드에서만** 한다(키 노출 금지).

| API | 용도 | 정상 호출 방식 |
| --- | --- | --- |
| KASI 음양력 변환 | 생년월일 → 음력 변환 | 실시간 |
| KASI 음양력 정보 | 연주(세차)·월주(24절기)·일주(일진) 조회 | 실시간 |
| TourAPI 웰니스관광정보 | 오행 매칭 추천 대상 스팟 | batch → Airtable |
| TourAPI 지역별 관광지 집중률 | 혼잡 스팟을 추천 후보에서 제외 | 실시간 |
| TourAPI 관광지별 연관 관광지 | 추천 스팟 주변 숙박/음식 동선 구성 | batch |
| TourAPI 기초지자체 중심 관광지 | 지방 분산용 지역 중심 관광지 | batch |
| 국문/다국어 관광정보 | 영어 대응 | 실시간 |
| 여기어때 어필리에이트 | 국내 숙소·레저/티켓 예약 딥링크 | 실시간 |

> TourAPI는 서비스마다 별도 신청·별도 오퍼레이션 ID(KorService1/2로 버전이 나뉨). 파라미터는 포털 상세에서 확정.

## 4. 도메인 모델 (용어 통일)

코드·주석·타입에서 아래 용어를 일관되게 쓴다.

- **오행 (five elements)**: `wood(木) | fire(火) | earth(土) | water(水) | metal(金)` — enum/유니온 타입으로 고정.
- **사주 프로필**: 연주·월주·일주에서 산출한 오행 분포. **시주는 제외**(태어난 시간 미입력, 유료 리포트 P2에서만).
- 장소 오행 필드(Airtable 원본 기준, 필드명은 상수로 매핑):
  - 원본: `name`, `region`, `contentId`, `category_code`, `firstimage` 등
  - 매핑: `attribute_element`(속성), `action_element`(행위), `primary_element`(종합 산출값)
- 오행-장소 매핑은 단일 기준이 아니라 **속성 레이어 + 행위 레이어를 겹쳐** 추천 다양성을 만든다.
  - 속성 예: 산=木, 온천/계곡=水, 찜질방=火, 갯벌/도예=土, 사찰 범종/공방=金
- 프레이밍 원칙: "효과가 검증됨"이 아니라 **"전통 오행 사상에서 ~한 공간이 좋다고 여겨진다"** 는 문화적 해석/놀이 톤으로 카피를 작성한다.
- **유저/회원 데이터**(회원가입 scope-in): 성별·생년월일(필수, pre-fill)과 사주 결과·저장한 일정·수집한 엘레먼트를 Airtable에 저장. 개인정보이므로 서버 사이드에서만 접근하고 클라이언트로 원본을 내리지 않는다(6.1). 개인정보처리방침·이용약관은 AI 생성.

## 5. 추천 로직 (F-3)

> PRD 6-1의 F-3 "추천 로직"은 공란이었다. 아래는 확정안이며, 결정 지점 2개(타깃 오행 범위·카피 프레임)는 각 절에 표시했다. 바꾸려면 해당 절만 수정한다.

### 5.1 파이프라인 개요
① 사주 프로필 산출 → ② 타깃 오행 결정 → ③ 후보 풀 조회(Airtable) → ④ 혼잡 실시간 필터 → ⑤ 지방·다양성 랭킹 → ⑥ 카테고리별 노출(F-3) → ⑦ 동선 담기(F-5). 로직은 `lib/recommend/`에 둔다.

### 5.2 사주 → 오행 분포 산출
- KASI로 연주(간지/세차)·월주(24절기)·일주(간지/일진) 산출. **시주 제외.**
- 각 기둥의 천간·지지를 오행으로 매핑 → **6글자 카운트**로 오행 분포 산출.
- **참조 기준표(천간·지지·24절기·간지→오행 매핑)는 자체 데이터로 둔다**(`config/` 상수 또는 Airtable). 단, 날짜→간지 변환 등 **실제 산출은 KASI 실시간 호출**로 한다(WBS "사주 데이터 자체 구축=외부 API X"는 *기준표 자체 구축*을 뜻하며, 산출까지 API를 안 쓴다는 의미가 아니다 — 공모전 실시간 호출 요건과 상충 방지).
- (P2) 지지 지장간 가중 등 정밀 산출은 유료 리포트에서.

### 5.3 타깃 오행 결정 — 확정: **결핍 + 과잉 둘 다**
- 오행 분포에서 **가장 부족한 원소(결핍)와 가장 넘치는 원소(과잉)를 모두 타깃**으로 잡는다.
  - 결핍 → "채우러 가는" 보완 추천, 과잉 → "이미 강한 기운과 공명하는" 증폭 추천. 둘 다 5.8 공명·어울림 톤으로 카피한다.
- 동점이면 고정 우선순위(config 상수)로 선택.
- F-5 일정 pre-fill은 PRD대로 **결핍 원소**를 채운다(수정 불가). 과잉 기반 추천은 F-3 탐색에서 함께 노출.
- 미채택 대안: 최다 결핍 1개만 / 결핍 전부. 변경 시 이 절만 수정.

### 5.4 후보 풀 — 레이어 매칭
- Airtable에서 타깃 오행(결핍·과잉)과 매칭되는 장소를 조회한다.
- 매칭은 `primary_element` 우선, **`attribute_element`(속성)·`action_element`(행위) 레이어도 후보에 포함** → 같은 타깃 오행에서도 장소 타입이 다양해진다(FAQ Q2).

### 5.5 혼잡 필터 & 지방 소싱
- **지역별 관광지 집중률 API(실시간, `no-store`)** 로 과밀 스팟을 후보에서 제외한다.
- 지방 후보는 **기초지자체 중심 관광지(batch)** 로 확보해 편중을 완화한다.

### 5.6 랭킹
- 정렬 가중치(모두 `config` 상수로 관리, 튜닝 가능):
  1. 오행 매칭 강도 — `primary` > `attribute`/`action`
  2. 비혼잡 가점 — 집중률 낮을수록 우대
  3. 지역 다양성 가점 — 지방 우대, 인기 편중 지역 감점
  4. 데이터 완성도 소폭 가점 — `firstimage`·예약 링크 유무

### 5.7 노출(F-3) & 동선(F-5)
- 결과를 카테고리(웰니스/체험/관광지 등)별로 그룹핑해 탐색(SRP).
- F-4 PDP: 추천 근거 카피 + 몰리는/여유로운 시간 + 예약 링크(여기어때 어필리에이트).
- F-5: 타깃 오행 pre-fill + 여행일자 → 일자별 담기, **관광지별 연관 관광지 API**로 주변 숙박/음식 동선 확장.

### 5.8 카피 프레임 — 확정: **공명·어울림 중심**
- 추천 근거·결과 카피는 **"너의 오행과 어울리는/공명하는 곳"** 을 기본 프레임으로 한다("당신의 물 기운과 공명하는 청계천 걷기" 류). 서양권 방어감이 적고 '효과 검증' 논란을 피한다(FAQ Q3·Q4).
- 개별 장소에 개운·효험 스토리(운을 열어준다/액운을 막는다 류)를 부가 양념으로 얹을 수 있으나, "효과가 검증됨"으로 단정하지 않는다(4번 프레이밍 원칙).
- 미채택 대안: 개운·효험 중심 / 둘 다. 변경 시 이 절 + 4번 조정.

## 6. 코딩 규칙

### 6.1 보안 / 비밀
- 모든 API 키·Airtable 토큰은 **환경변수**로만 접근(`process.env.*`). `.env.local`은 커밋 금지, Vercel Environment Variables 사용.
- **외부 API·Airtable 호출은 Server Component 또는 Route Handler(`app/api/*`)에서만.** 클라이언트 컴포넌트에서 직접 호출 금지(키·CORS·비용 노출).
- 클라이언트로 내려가는 값에 원본 키/내부 필드가 섞이지 않게 서버에서 정제 후 전달.

### 6.2 타입
- `tsconfig` strict. `any` 금지, 불가피하면 `unknown` + 좁히기.
- 오행·사주·장소 등 도메인 타입은 `types/`에 단일 정의하고 재사용.
- Airtable 원본 필드명 문자열을 코드에 흩뿌리지 말고 `config/`의 상수 맵으로 관리.

### 6.3 UI / 카피
- 화면 노출 텍스트는 **영어**. 코드 주석·커밋은 한국어 허용.
- 사주 결과·추천 근거 카피는 4번의 문화적 해석 톤과 5.8 프레임(공명·어울림)을 지킨다("당신의 물 기운과 공명하는 곳" 류).

### 6.4 언어(한국어 헤더 규칙)
- 문서 헤더는 sentence case.

### 6.5 캐싱 (Vercel 서버리스 대응 — PRD와 다른 점)
- PRD의 "서버 시작 시 Airtable 전체 레코드 1회 조회 → 전역 메모리 캐싱"은 **상시 구동 서버 전제**다. Vercel 서버리스는 요청 간 메모리가 유지되지 않으므로 **전역 변수 메모리 캐시에 의존하지 말 것.**
- 대신 Next.js 데이터 캐시를 쓴다: `unstable_cache` / `fetch(..., { next: { revalidate } })` / Route Segment `revalidate`. 배치 데이터(웰니스 등)는 긴 revalidate, 실시간 데이터(집중률 등)는 캐시 없음(`no-store`).
- **심사 기간 실시간 모드**: `REALTIME_API_MODE=true`이면 배치성 데이터도 캐시를 우회해 실시간 호출하도록 데이터 접근 레이어에서 분기한다.

### 6.6 데이터 파이프라인 규칙
- 배치 업데이트(Apps Script): TourAPI 1일 1회 호출 → `contentId` 기준 대조.
  - 기존 레코드: **원본 필드만 덮어쓰기**(무조건). **오행 매핑 값(`attribute/action/primary_element`)은 절대 덮어쓰지 않는다.**
  - 신규 레코드: 추가 후 **매핑 요청 알림**.
- 유저 요청 파이프라인: 요청마다 필요한 실시간 API 호출 → 서버 BE에서 Airtable 조회(오행 필드 참조) → 정제 후 응답.

## 7. 폴더 구조

```
/
├── app/                      # Next.js App Router
│   ├── layout.tsx
│   ├── page.tsx              # F-1 홈/랜딩 (성별·생년월일 입력)
│   ├── result/               # F-2 사주 결과 (부족/넘치는 기운, K스타)
│   ├── explore/              # F-3 카테고리별 추천 탐색 (SRP)
│   ├── place/[contentId]/    # F-4 PDP (근거·혼잡시간·예약링크)
│   ├── plan/                 # F-5 일정 만들기 (동선 설정)
│   └── api/                  # Route Handlers (외부 API 프록시 / BFF)
│       ├── saju/             #   KASI 변환·정보 → 사주 산출
│       ├── places/           #   Airtable 조회 + 집중률 필터
│       └── ...
├── components/               # 재사용 UI 컴포넌트
├── lib/                      # 도메인 로직 (서버)
│   ├── saju/                 #   KASI 변환, 간지 계산, 오행 프로필 산출
│   ├── airtable/             #   Airtable 클라이언트·쿼리·필드 매핑
│   ├── tour-api/             #   TourAPI(웰니스/집중률/연관/다국어) 클라이언트
│   └── recommend/            #   오행-장소 매핑, 추천·필터 로직
├── types/                    # 공용 도메인 타입 (오행·사주·장소·일정)
├── config/                   # 상수 (오행 매핑 테이블, Airtable 필드명, env 스키마)
├── scripts/
│   └── apps-script/          # Google Apps Script 배치 원본(버전 관리용)
├── public/
├── .env.example              # 필요한 env 키 목록(값은 비움)
└── CLAUDE.md
```

## 8. 목표 지표

> PRD 5장(목표 지표)은 공란이었다. 아래는 확정안. North Star는 **미션형**, 구체 목표 수치는 베이스라인(론칭 초기 데이터) 확보 후 채운다(현재 TBD). 결정 지점은 표시했다.

### 8.1 North Star Metric — 확정: **지방 포함 완성 일정 수**
- **지방(비수도권·저집중률) 스팟이 1개 이상 포함된 완성 일정 수.**
- 오버투어리즘 해결이라는 공모전 핵심 논지와 직결되고, 어필리에이트 전환의 선행지표.
- 미채택 대안: 어필리에이트 전환(수익형) / 여정 지속 활성유저(참여형). 변경 시 이 절 수정.

### 8.2 심사 시점 지표 (9/21, 트래픽 전 — 기능·정성)
- 실시간 API 정상 동작(집중률·KASI·다국어 등) — 공모전 필수 요건 충족 여부(pass/fail)
- KTO 필수 API 활용 수(웰니스+지역기반+다국어 최소 3종) 충족
- 데모 시나리오 완주: 사주 입력 → 추천 → 일정 생성 → 공유 콘텐츠까지 무결
- 추천 결과의 지방 스팟 비율(데모 데이터 기준)

### 8.3 론칭 후 운영 KPI (수치 목표는 TBD)
- **미션/분산**: 추천·일정 내 지방 스팟 비율, 편중 랜드마크(명동·강남·경복궁 등) 회피율
- **활성 퍼널**: 랜딩→사주입력 완료율 → 추천 탐색 진입율 → PDP 조회율 → 일정 담기율 → 일정 완성율
- **여정 지속(전/중/후)**: 여행 중 체크인율, 여행 후 공유 콘텐츠 생성율·SNS 공유율, 공유 재유입(바이럴 K)
- **비즈니스**: 어필리에이트 딥링크 CTR·예약 전환·커미션, 배너 광고 노출·수익

### 8.4 계측 (개발 반영)
- 위 퍼널·전환 지표는 이벤트 로깅으로 계측한다. 비로그인 단계도 익명 세션 기준으로 퍼널을 추적.
- 공유·어필리에이트 딥링크에는 UTM/식별 파라미터를 부여해 재유입·전환을 귀속한다.
- 계측 도구(GA4 등)는 추후 확정.

## 9. 확정 대기 항목 (PRD상 미정 — 결정 후 이 파일 갱신)

- **스타일링 스택**(Tailwind 등): 8/12 스펙 확정 미팅에서 결정.
- **검색 UX**(F-5 P1): 검색이 필요한 UX인지 미확정. 도입 시 Airtable `filterByFormula` 사용(Elasticsearch는 오버스펙).
